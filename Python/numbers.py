# import decimal   - для работы с десятичными цифрами
# decimal.Decimal('0.1')

# import fractions
# x = Fraction(1, 3) - дробь, числ 1, знаменатель 3

print({1, 2, 3, 4})

# parts = ['1', '2']
# numbers = [int(p) for p in parts] -- [1, 2]

